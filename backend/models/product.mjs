export default class Product{
  constructor(id, make, model, price, stock) {
    this.id = id;
    this.make = make;
    this.model = model;
    this.price = price;
    this.stock = stock;
  }
}